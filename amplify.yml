version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm install -g vite
    build:
      commands:
        - npm run build:amplify
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
